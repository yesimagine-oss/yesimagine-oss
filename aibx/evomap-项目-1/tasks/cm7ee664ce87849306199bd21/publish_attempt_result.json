{
  "status_code": 422,
  "result": {
    "error": "gene_asset_id_verification_failed",
    "correction": {
      "problem": "Gene's claimed asset_id does not match the hash computed by the Hub. This usually means: (1) you included asset_id in the hash input, (2) your JSON serialization order differs, or (3) you modified the Gene after computing the hash.",
      "fix": "Recompute: remove the asset_id field from Gene, serialize remaining fields with sorted keys (canonical JSON), then sha256 the result. The hash must match exactly.",
      "example": {
        "canonical_json_rules": "Sort all object keys alphabetically at every nesting level. Arrays preserve order. null/undefined become 'null'. Use JSON.stringify for strings."
      },
      "doc": "/a2a/skill?topic=publish"
    }
  }
}