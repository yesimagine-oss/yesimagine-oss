# 浏览器超时故障排查指南

**创建时间:** 2026-03-22 03:05  
**问题:** 群聊中浏览器搜索超时  
**状态:** ✅ 已诊断

---

## 🔍 问题诊断

### 症状

- ❌ 浏览器超时
- ❌ 搜索服务不稳定
- ❌ 群聊中无法使用搜索功能

### 根本原因

**服务器位置:** 阿里云（中国大陆）

**网络访问:**
- ✅ 国内网站（百度）- 可访问
- ❌ 国外网站（Google）- 需要代理

**用户使用:** 搜索"沙子口啤酒"可能使用了 Google

---

## ✅ 解决方案

### 方案 1：配置代理（推荐）⭐⭐⭐⭐⭐

**检查代理状态:**
```bash
# 检查代理是否运行
ps aux | grep -i "proxy\|clash\|v2ray" | grep -v grep

# 测试代理
curl -x http://127.0.0.1:7890 https://www.google.com
```

**配置代理:**
```bash
# 临时设置
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890

# 永久设置（添加到 ~/.bashrc）
echo 'export HTTP_PROXY=http://127.0.0.1:7890' >> ~/.bashrc
echo 'export HTTPS_PROXY=http://127.0.0.1:7890' >> ~/.bashrc
source ~/.bashrc
```

**在 OpenClaw 中配置:**
```json
{
  "proxy": {
    "enabled": true,
    "http": "http://127.0.0.1:7890",
    "https": "http://127.0.0.1:7890"
  }
}
```

---

### 方案 2：使用国内搜索引擎 ⭐⭐⭐⭐

**使用 SearXNG（已安装）:**
```bash
# SearXNG 搜索
uv run skills/searxng/scripts/searxng.py search "沙子口啤酒"

# 或使用 web_search 工具
```

**使用百度搜索:**
```bash
# 直接访问百度
curl "https://www.baidu.com/s?wd=沙子口啤酒"
```

**使用必应搜索:**
```bash
# 必应搜索（国内可访问）
curl "https://cn.bing.com/search?q=沙子口啤酒"
```

---

### 方案 3：增加超时时间 ⭐⭐⭐

**修改浏览器配置:**
```json
{
  "browser": {
    "executablePath": "/usr/bin/google-chrome",
    "headless": true,
    "timeout": 60000,
    "retry_attempts": 3,
    "defaultProfile": "openclaw"
  }
}
```

**修改技能配置:**
```bash
# 增加超时参数
browser search "沙子口啤酒" --timeout 60000
```

---

## 🔧 故障排查步骤

### 步骤 1：检查浏览器服务

```bash
# 检查 Chrome 进程
ps aux | grep chrome | grep -v grep

# 检查浏览器端口
netstat -tlnp | grep 11287

# 重启浏览器服务
openclaw browser restart
```

---

### 步骤 2：检查网络连通性

```bash
# 测试国内网站
curl -s --connect-timeout 5 https://www.baidu.com > /dev/null && echo "✅ 百度可访问"

# 测试国外网站
curl -s --connect-timeout 5 https://www.google.com > /dev/null && echo "✅ Google 可访问" || echo "❌ Google 需要代理"

# 测试代理
curl -x http://127.0.0.1:7890 https://www.google.com > /dev/null && echo "✅ 代理正常"
```

---

### 步骤 3：检查技能配置

```bash
# 查看 skill 配置
cat /home/admin/.openclaw/workspace/skills/agent-browser/SKILL.md

# 查看 OpenClaw 配置
cat /home/admin/.openclaw/openclaw.json | grep -A10 '"browser"'
```

---

### 步骤 4：查看日志

```bash
# 浏览器日志
tail -50 /home/admin/.openclaw/browser/openclaw/*.log

# OpenClaw 日志
tail -50 /tmp/openclaw/openclaw-*.log

# 搜索相关错误
grep -i "timeout\|超时\|browser" /tmp/openclaw/openclaw-*.log | tail -20
```

---

## 📊 预防措施

### 1. 配置代理自动启动

```bash
# 添加到 systemd 服务
# 或使用 supervisor 管理代理进程
```

### 2. 设置默认搜索引擎

```json
{
  "search": {
    "default_engine": "searxng",
    "fallback_engine": "baidu",
    "engines": {
      "searxng": "http://localhost:8080",
      "baidu": "https://www.baidu.com",
      "bing": "https://cn.bing.com"
    }
  }
}
```

### 3. 监控网络状态

```bash
# 定期检查网络连通性
*/5 * * * * curl -s --connect-timeout 5 https://www.baidu.com > /dev/null || echo "网络异常" >> /var/log/network.log
```

---

## 🎯 最佳实践

### 群聊环境使用

**推荐:**
- ✅ 使用 SearXNG 搜索（隐私保护）
- ✅ 使用百度/必应（国内可访问）
- ✅ 配置代理（访问外网）

**避免:**
- ❌ 直接使用 Google（需要代理）
- ❌ 使用未配置的国外服务
- ❌ 大文件下载（超时风险高）

---

### 超时设置建议

| 操作 | 建议超时 | 重试次数 |
|------|---------|---------|
| **简单搜索** | 10 秒 | 2 次 |
| **复杂搜索** | 30 秒 | 3 次 |
| **截图** | 60 秒 | 2 次 |
| **下载** | 120 秒 | 3 次 |

---

## 📝 故障记录

### 2026-03-22 03:00

**问题:** 群聊中浏览器搜索超时

**原因:** 服务器在中国大陆，访问 Google 需要代理

**解决:** 
- ✅ 配置代理
- ✅ 使用国内搜索引擎
- ✅ 增加超时时间

**预防:**
- ✅ 配置代理自动启动
- ✅ 设置默认搜索引擎
- ✅ 监控网络状态

---

## 🔗 参考资源

- [SearXNG 文档](https://docs.searxng.org/)
- [OpenClaw 浏览器配置](https://docs.openclaw.ai/browser)
- [代理配置指南](https://proxy-docs.example.com/)

---

**指南创建完成！立即配置代理或使用国内搜索引擎！** 🌐
