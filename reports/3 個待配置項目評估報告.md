# 3 個待配置項目評估報告

**評估時間**: 2026-03-19 12:22 GMT+8  
**評估者**: RedOpenClaw

---

## 📊 總體評估

| 項目 | 依賴 | 難度 | 可獨立完成 | 預估時長 |
|------|------|------|-----------|---------|
| brave-search | BRAVE_API_KEY | ⭐ 簡單 | ✅ 是 | 10 分鐘 |
| caldav-calendar | vdirsyncer/khal | ⭐⭐⭐ 複雜 | ❌ 需協助 | 60 分鐘 |
| qmd | TypeScript/bun | ⭐⭐ 中等 | ✅ 是 | 30 分鐘 |

---

## 1️⃣ brave-search (Brave Search API)

### 📋 項目說明

**用途**: 使用 Brave Search API 進行網頁搜索  
**依賴**: BRAVE_API_KEY (API 密鑰)  
**配置方式**: 環境變量

### 🔧 配置方案

```bash
# 1. 獲取 API Key
# 訪問：https://brave.com/search/api/
# 註冊賬戶，創建 API Key (免費層：每月 2000 次請求)

# 2. 配置環境變量
# 方法 A: 臨時配置
export BRAVE_API_KEY="your-api-key-here"

# 方法 B: 永久配置 (~/.bashrc 或 ~/.zshrc)
echo 'export BRAVE_API_KEY="your-api-key-here"' >> ~/.bashrc
source ~/.bashrc

# 方法 C: Docker 環境變量
docker run -e BRAVE_API_KEY="your-api-key" ...

# 方法 D: OpenClaw 配置
openclaw configure --section web
```

### ✅ 我是否可以獨立完成

**答案**: ✅ **可以，但需要用戶提供 API Key**

**我能做的**:
- [x] 配置環境變量
- [x] 測試 API 連接
- [x] 集成到 OpenClaw

**需要用戶提供的**:
- [ ] Brave Search API Key (需註冊賬戶獲取)

### 📝 配置步驟 (獲得 API Key 後)

```bash
# 1. 配置環境變量
export BRAVE_API_KEY="BSAxxxxxxxxxxxxxxxxxx"

# 2. 測試 API
curl -s "https://api.search.brave.com/res/v1/web/search?q=test" \
  -H "Accept: application/json" \
  -H "Accept-Encoding: gzip" \
  -H "X-Subscription-Token: $BRAVE_API_KEY" | head -20

# 3. 添加到 OpenClaw 配置
echo "BRAVE_API_KEY=$BRAVE_API_KEY" >> ~/.openclaw/.env
```

### 💰 費用說明

| 層級 | 價格 | 請求次數 |
|------|------|---------|
| Free | $0/月 | 2,000 次 |
| Basic | $3/月 | 20,000 次 |
| Standard | $25/月 | 200,000 次 |
| Pro | $250/月 | 2,000,000 次 |

---

## 2️⃣ caldav-calendar (CalDAV 日曆同步)

### 📋 項目說明

**用途**: 同步 CalDAV 日曆 (Google Calendar, iCloud, Nextcloud 等)  
**依賴**: 
- vdirsyncer (日曆同步工具)
- khal (日曆查看器)
- CalDAV 服務器賬戶

**配置方式**: 配置文件 + 命令安裝

### 🔧 配置方案

```bash
# 1. 安裝依賴
# Ubuntu/Debian
sudo apt install vdirsyncer khal

# macOS
brew install vdirsyncer khal

# 或使用 pip
pip install vdirsyncer khal

# 2. 配置 vdirsyncer
mkdir -p ~/.config/vdirsyncer
cat > ~/.config/vdirsyncer/config <<EOF
[general]
status_path = "~/.vdirsyncer/status/"

[pair my_calendar]
a = my_calendar_local
b = my_calendar_remote
collections = ["from config"]

[storage my_calendar_local]
type = "filesystem"
path = "~/.calendars"
fileext = ".ics"

[storage my_calendar_remote]
type = "caldav"
url = "https://caldav.example.com/"
username = "your-username"
password.fetch = ["command", "pass", "show", "caldav-password"]
EOF

# 3. 同步日曆
vdirsyncer discover
vdirsyncer sync

# 4. 查看日曆
khal list
```

### ✅ 我是否可以獨立完成

**答案**: ❌ **需要用戶協助提供賬戶信息**

**我能做的**:
- [x] 安裝 vdirsyncer/khal
- [x] 創建配置文件
- [x] 配置同步腳本
- [x] 測試連接

**需要用戶提供的**:
- [ ] CalDAV 服務器 URL
- [ ] 用戶名
- [ ] 密碼/授權碼
- [ ] 確認使用哪個日曆服務 (Google/iCloud/Nextcloud/其他)

### 📝 配置步驟

```bash
# 1. 安裝依賴 (我可以獨立完成)
sudo apt install vdirsyncer khal

# 2. 配置 (需要用戶提供信息)
# Google Calendar:
#   URL: https://www.google.com/calendar/dav/
#   用戶名：your-email@gmail.com
#   密碼：應用專用密碼 (需開啟 2FA)

# iCloud:
#   URL: https://caldav.icloud.com/
#   用戶名：your-apple-id
#   密碼：應用專用密碼

# 3. 同步
vdirsyncer sync
```

### ⚠️ 注意事項

1. **密碼安全**: 建議使用密碼管理器存储
2. **雙因素認證**: 需要生成應用專用密碼
3. **隱私考慮**: 日曆數據會同步到本地

---

## 3️⃣ qmd (Quarto Markdown)

### 📋 項目說明

**用途**: Quarto 文檔處理 (Markdown 增強版，支持代碼、圖表、出版)  
**依賴**: 
- Bun (JavaScript 運行時) 或 Node.js
- TypeScript
- Quarto CLI (可選)

**配置方式**: 安裝運行時 + 構建項目

### 🔧 配置方案

```bash
# 1. 安裝 Bun (推薦)
curl -fsSL https://bun.sh/install | bash

# 或安裝 Node.js (已安裝)
# node --version  # 檢查版本

# 2. 安裝 Quarto CLI
# macOS
brew install quarto

# Linux
wget -q https://github.com/quarto-dev/quarto-cli/releases/download/v1.4.546/quarto-1.4.546-linux-amd64.tar.gz
tar -xzf quarto-1.4.546-linux-amd64.tar.gz
sudo mv quarto-1.4.546/bin/quarto /usr/local/bin/

# 3. 配置 qmd 項目
mkdir -p ~/qmd-project
cd ~/qmd-project

# 初始化項目
bun init
bun add typescript @types/node

# 4. 構建
bun run build
```

### ✅ 我是否可以獨立完成

**答案**: ✅ **完全可以獨立完成**

**我能做的**:
- [x] 安裝 Bun/Node.js
- [x] 安裝 TypeScript
- [x] 配置 qmd 項目
- [x] 創建構建腳本
- [x] 測試運行

**需要用戶提供的**:
- [ ] 無 (可獨立完成)

### 📝 配置步驟 (我可自動執行)

```bash
# 1. 檢查並安裝 Bun
if ! command -v bun &> /dev/null; then
  curl -fsSL https://bun.sh/install | bash
fi

# 2. 安裝 TypeScript
bun add -d typescript @types/node

# 3. 創建項目結構
mkdir -p ~/qmd-project/{src,dist}
cd ~/qmd-project

# 4. 創建 tsconfig.json
cat > tsconfig.json <<EOF
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true
  }
}
EOF

# 5. 創建 package.json
cat > package.json <<EOF
{
  "name": "qmd-project",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  }
}
EOF

# 6. 測試構建
bun run build
```

### 💰 費用說明

- **Bun**: ✅ 免費開源
- **TypeScript**: ✅ 免費開源
- **Quarto CLI**: ✅ 免費開源

---

## 📊 綜合建議

### 優先級排序

1. **qmd** (優先級：高)
   - ✅ 可獨立完成
   - 💰 免費
   - ⏱️ 30 分鐘

2. **brave-search** (優先級：中)
   - ✅ 可獨立完成 (需 API Key)
   - 💰 免費層 2000 次/月
   - ⏱️ 10 分鐘

3. **caldav-calendar** (優先級：低)
   - ❌ 需用戶提供賬戶信息
   - 💰 免費 (取決於日曆服務)
   - ⏱️ 60 分鐘

### 執行建議

**立即執行** (無需用戶協助):
```bash
# 1. 配置 qmd
我可以立即自動執行
```

**需要用戶提供信息**:
```bash
# 2. brave-search
請提供：Brave Search API Key (或我指導你獲取)

# 3. caldav-calendar
請提供：
- 日曆服務類型 (Google/iCloud/Nextcloud/其他)
- CalDAV URL
- 用戶名
- 密碼/授權碼
```

---

## 🎯 下一步行動

### 選項 A: 先配置 qmd (推薦)
- 無需用戶協助
- 我可立即執行
- 約 30 分鐘完成

### 選項 B: 同時配置 3 個
- qmd: 我立即執行
- brave-search: 你提供 API Key
- caldav-calendar: 你提供日曆信息

### 選項 C: 暫不配置
- 保持現狀
- 需要時再配置

---

**請選擇下一步行動！** 🚀

**評估時間**: 2026-03-19 12:22 GMT+8
