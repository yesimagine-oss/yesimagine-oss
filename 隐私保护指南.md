# 隐私保护指南

**创建时间:** 2026-03-21 23:10  
**级别:** 🔴 最高优先级  
**状态:** ✅ 已建立

---

## 🚨 隐私泄露事故

**时间:** 2026-03-21 23:10  
**级别:** P0 特大事故  
**问题:** 飞书群消息中暴露用户 OpenID

**泄露内容:**
```
（老胡你刚才@的是 `ou_382066265e530e2faad2433635178494`，这个 ID
我这边还没备注名字，是"机械师"本人吗？）
```

---

## 🛡️ 隐私保护原则

### 1. 禁止暴露用户 ID

**❌ 错误:**
```python
print(f"用户 ID: {user_id}")
print(f"@{user_name} ({user_id})")
```

**✅ 正确:**
```python
# 仅在内部使用，不打印
# 或使用昵称
print(f"@{user_name}")
```

---

### 2. 敏感信息脱敏

**需要脱敏的信息:**
- ❌ 用户 OpenID (ou_xxxxx)
- ❌ 用户手机号
- ❌ 用户邮箱
- ❌ 用户地址
- ❌ API Key/Token
- ❌ 密码/密钥

**脱敏方式:**
```python
# 完全隐藏
print("用户 ID: ***")

# 部分隐藏
print(f"用户 ID: ou_{'*' * 20}")

# 使用昵称
print(f"用户：{nickname}")
```

---

### 3. 群聊环境特别注意

**群聊中禁止:**
- ❌ 发送用户 ID
- ❌ 发送敏感配置
- ❌ 发送个人隐私信息
- ❌ 发送未脱敏的数据

**群聊中可以:**
- ✅ 使用昵称
- ✅ 发送公开信息
- ✅ 发送脱敏后的数据

---

### 4. 日志与公开消息分离

**日志中可以记录:**
```python
# ✅ 日志（不公开）
logger.info(f"发送给用户 {user_id} 的通知")
```

**公开消息:**
```python
# ✅ 公开消息（不显示 ID）
send_message("已发送通知")
```

---

## 🔧 已修复的文件

| 文件 | 修复内容 | 状态 |
|------|---------|------|
| **feishu-group-integration.py** | 移除成员列表中的 ID 显示 | ✅ 已修复 |
| **feishu-mention.py** | 移除提及详情中的 ID 显示 | ✅ 已修复 |
| **task-notifier.py** | 仅@功能使用 ID，不暴露 | ✅ 已检查 |
| **其他工具** | 全面检查中 | ⏳ 进行中 |

---

## 📋 检查清单

### 代码审查

- [ ] 检查所有 print 语句
- [ ] 检查所有消息发送函数
- [ ] 检查所有日志输出
- [ ] 检查所有配置文件
- [ ] 检查所有缓存文件

### 工具检查

- [ ] task-notifier.py
- [ ] learning-watcher.py
- [ ] python-learning-notifier.py
- [ ] feishu-mention.py
- [ ] feishu-group-integration.py
- [ ] 其他飞书相关工具

### 配置检查

- [ ] 配置文件中的敏感信息
- [ ] 环境变量中的敏感信息
- [ ] 缓存文件中的敏感信息
- [ ] 日志文件中的敏感信息

---

## 🎯 最佳实践

### 1. 使用配置管理敏感信息

```python
# ✅ 从配置文件加载
config = load_config()
user_id = config.get('target_user')

# ❌ 硬编码
user_id = "ou_xxxxx"
```

### 2. 使用环境变量

```bash
# ✅ 环境变量
export FEISHU_USER_ID="ou_xxxxx"
```

```python
# ✅ 代码中使用
user_id = os.getenv('FEISHU_USER_ID')
```

### 3. 日志分级

```python
# ✅ DEBUG 级别记录敏感信息
logger.debug(f"用户 ID: {user_id}")

# ✅ INFO 级别不记录敏感信息
logger.info(f"发送通知给用户")
```

### 4. 数据脱敏函数

```python
def mask_sensitive_data(data, mask_char='*', visible_length=4):
    """脱敏敏感数据"""
    if len(data) <= visible_length:
        return mask_char * len(data)
    return data[:visible_length] + mask_char * (len(data) - visible_length)

# 使用
masked_id = mask_sensitive_data(user_id)
print(f"用户 ID: {masked_id}")
```

---

## 📝 事故教训

### 不要做的（❌）

1. ❌ 在公开消息中暴露用户 ID
2. ❌ 在群聊中发送敏感信息
3. ❌ 打印完整用户 ID
4. ❌ 忽视隐私保护
5. ❌ 日志与公开消息混用

### 要做的（✅）

1. ✅ 用户 ID 脱敏处理
2. ✅ 群聊环境特别注意
3. ✅ 使用昵称代替 ID
4. ✅ 日志与公开消息分离
5. ✅ 定期审查代码
6. ✅ 隐私保护培训

---

## 🔗 参考资源

- [飞书开放平台隐私保护](https://open.feishu.cn/document/privacy)
- [GDPR 合规指南](https://gdpr.eu/)
- [隐私保护最佳实践](https://owasp.org/www-project-privacy-control/)

---

**隐私保护指南创建完成！立即执行全面检查！** 🔒
