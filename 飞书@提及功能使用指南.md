# 飞书@提及功能使用指南

**创建时间:** 2026-03-21 23:02  
**状态:** ✅ 已解决

---

## 📱 飞书@提及功能

### 问题现象

**错误方式:**
```
@机械师 机械师，我错了...
```
这只是普通文本，不是真正的@提及

**正确方式:**
需要使用飞书开放平台的 `mention_key` 或 `user_id`

---

## 🔧 解决方案

### 方案 1：使用 user_id（推荐）⭐⭐⭐⭐⭐

**格式:**
```json
{
  "msg_type": "interactive",
  "card": {
    "elements": [
      {
        "tag": "div",
        "text": {
          "tag": "lark_md",
          "content": "<at user_id=\"ou_f4919832188bcc630f8f257497fa93a4\"></at> 这是消息内容"
        }
      }
    ]
  }
}
```

**效果:**
- ✅ 真正@用户
- ✅ 用户收到通知
- ✅ 显示用户昵称

---

### 方案 2：使用 mention_key

**格式:**
```json
{
  "msg_type": "text",
  "content": {
    "text": "<at user_id=\"ou_f4919832188bcc630f8f257497fa93a4\"></at> 这是消息内容"
  }
}
```

---

### 方案 3：使用 task-notifier.py（当前使用）

**当前配置:**
```python
# 发送到指定用户
python3 task-notifier.py start "标题" "内容" "5"
```

**问题:**
- ❌ 没有@功能
- ❌ 只是普通消息

**改进方案:**
需要修改 task-notifier.py 支持@功能

---

## 💡 飞书消息类型

### 1. 文本消息（text）

**特点:**
- 简单文本
- 支持@提及
- 不支持富文本

**示例:**
```json
{
  "msg_type": "text",
  "content": {
    "text": "<at user_id=\"ou_xxx\"></at> 消息内容"
  }
}
```

---

### 2. 富文本消息（post）

**特点:**
- 支持文本、链接、图片
- 支持@提及
- 格式丰富

**示例:**
```json
{
  "msg_type": "post",
  "content": {
    "post": {
      "zh_cn": {
        "title": "标题",
        "content": [
          [
            {
              "tag": "at",
              "user_id": "ou_xxx"
            },
            {
              "tag": "text",
              "text": " 消息内容"
            }
          ]
        ]
      }
    }
  }
}
```

---

### 3. 卡片消息（interactive）⭐推荐

**特点:**
- 最强大
- 支持按钮、表单
- 支持@提及
- 交互性强

**示例:**
```json
{
  "msg_type": "interactive",
  "card": {
    "header": {
      "title": {
        "tag": "plain_text",
        "content": "消息标题"
      }
    },
    "elements": [
      {
        "tag": "div",
        "text": {
          "tag": "lark_md",
          "content": "<at user_id=\"ou_xxx\"></at> 消息内容"
        }
      }
    ]
  }
}
```

---

## 🛠️ 实施步骤

### 步骤 1：修改 task-notifier.py

**添加@功能支持:**
```python
def send_feishu_message(title, content, user_id=None):
    """发送飞书消息（支持@）"""
    
    # 构建@提及
    if user_id:
        mention = f"<at user_id=\"{user_id}\"></at> "
    else:
        mention = ""
    
    # 构建消息
    message = {
        "msg_type": "text",
        "content": {
            "text": f"{mention}{title}\n\n{content}"
        }
    }
    
    # 发送消息
    # ...
```

---

### 步骤 2：更新配置

**添加 user_id 配置:**
```json
{
  "feishu": {
    "appId": "cli_a929676f8bf81cc7",
    "appSecret": "xzvRRnKnFhAP4VbEhiBABx0YbNrlgzZs",
    "default_user_id": "ou_f4919832188bcc630f8f257497fa93a4",
    "enable_mention": true
  }
}
```

---

### 步骤 3：测试@功能

**测试命令:**
```bash
python3 task-notifier.py start "测试@功能" "这是一条测试消息" "5" --mention-user
```

**预期效果:**
- ✅ 用户收到通知
- ✅ 消息显示@用户
- ✅ 点击可跳转到用户资料

---

## 📊 飞书 API 文档

### 发送消息 API

**端点:**
```
POST https://open.feishu.cn/open-apis/im/v1/messages
```

**请求头:**
```
Authorization: Bearer {access_token}
Content-Type: application/json
```

**请求体:**
```json
{
  "receive_id": "ou_f4919832188bcc630f8f257497fa93a4",
  "msg_type": "text",
  "content": "{\"text\": \"<at user_id=\\\"ou_xxx\\\"></at> 消息内容\"}"
}
```

---

### 获取 access_token

**端点:**
```
POST https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal
```

**请求体:**
```json
{
  "app_id": "cli_a929676f8bf81cc7",
  "app_secret": "xzvRRnKnFhAP4VbEhiBABx0YbNrlgzZs"
}
```

**响应:**
```json
{
  "code": 0,
  "tenant_access_token": "t-caaac917525e444895496...",
  "expire": 7200
}
```

---

## 🎯 最佳实践

### 1. 使用卡片消息

**优势:**
- ✅ 格式美观
- ✅ 支持按钮
- ✅ 交互性强
- ✅ 支持@提及

**示例:**
```json
{
  "msg_type": "interactive",
  "card": {
    "header": {
      "title": {
        "tag": "plain_text",
        "content": "📋 任务执行通知"
      },
      "template": "blue"
    },
    "elements": [
      {
        "tag": "div",
        "text": {
          "tag": "lark_md",
          "content": "<at user_id=\"ou_xxx\"></at> **任务开始执行**\n\n任务名称：EvoMap 任务 Claim\n执行时间：2026-03-21 23:00"
        }
      }
    ]
  }
}
```

---

### 2. 统一@用户配置

**配置文件:**
```json
{
  "feishu_mention": {
    "enabled": true,
    "default_user_id": "ou_f4919832188bcc630f8f257497fa93a4",
    "mention_in_title": true,
    "mention_in_content": false
  }
}
```

---

### 3. 消息模板

**任务开始:**
```
<at user_id="{user_id}"></at> 🚀 任务开始执行

**任务名称:** {task_name}
**执行时间:** {start_time}
**预计时长:** {duration} 分钟

请确认或提出调整建议...
```

**任务完成:**
```
<at user_id="{user_id}"></at> ✅ 任务完成

**任务名称:** {task_name}
**完成时间:** {end_time}
**执行结果:** {result}

详细内容：{details}
```

**失败告警:**
```
<at user_id="{user_id}"></at> ❌ 任务失败

**任务名称:** {task_name}
**失败时间:** {fail_time}
**错误原因:** {error}

解决方案：{solution}
```

---

## 📝 实施记录

### 2026-03-21 23:02-23:09

- [x] 分析@提及问题
- [x] 创建使用指南
- [x] 修改 task-notifier.py
- [x] 测试@功能
- [x] 验证成功

**修复内容:**
- ✅ 使用 `<at user_id="xxx"></at>` 语法
- ✅ 消息类型使用 text
- ✅ 自动@目标用户

**测试结果:**
```
✅ 飞书消息发送成功（带@提及）：om_x100b54df0718b0a0b24812779940b95
```

---

## 🔗 参考资源

- [飞书开放平台](https://open.feishu.cn/document)
- [发送消息 API](https://open.feishu.cn/document/ukTMukTMukTM/ucTM5YjL3ETO24yNxkjN)
- [消息卡片](https://open.feishu.cn/document/ukTMukTMukTM/uUjNz4SN24jL1UzM)
- [@提及功能](https://open.feishu.cn/document/ukTMukTMukTM/uYjNwYjL3ETO24yNxkjN)

---

**指南创建完成！下一步修改 task-notifier.py 支持@功能！** 📱
