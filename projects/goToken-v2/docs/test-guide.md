# goToken-v2 测试指南

**版本**: v2.0.0  
**创建时间**: 2026-04-22

---

## 📁 测试文件位置

```
goToken-v2/test/
├── test_suite.go       # 测试套件代码
├── run_tests.sh        # 测试运行脚本
└── test_report.json    # 测试报告（运行后生成）
```

---

## 🧪 测试用例清单

| # | 测试用例 | 类型 | 说明 |
|---|---------|------|------|
| 1 | 缓存命中测试 | 必须 | 验证相同问题返回缓存答案 |
| 2 | 缓存未命中测试 | 必须 | 验证不同问题不命中缓存 |
| 3 | TTL 过期测试 | 必须 | 验证缓存过期机制 |
| 4 | 限流保护测试 | 必须 | 验证并发限流功能 |
| 5 | 空查询处理测试 | 必须 | 验证空输入处理 |
| 6 | 配置加载测试 | 必须 | 验证配置文件读取 |

---

## 🚀 运行测试

### 方法 1: 使用脚本（推荐）

```bash
cd /home/admin/.openclaw/workspace/goToken-v2/test
bash run_tests.sh
```

### 方法 2: 直接运行

```bash
cd /home/admin/.openclaw/workspace/goToken-v2/test
go run test_suite.go
```

---

## 📊 测试报告

**运行后生成**：
- 控制台输出（实时查看）
- `test_report.json`（JSON 格式）
- `test_report_table.csv`（表格格式）

**示例输出**：
```
🧪 goToken-v2 测试套件
================================
✅ 缓存命中测试 (0.01s)
✅ 缓存未命中测试 (0.00s)
✅ TTL 过期测试 (1.02s)
✅ 限流保护测试 (0.05s)
✅ 空查询处理测试 (0.00s)
✅ 配置加载测试 (0.01s)

================================
📊 测试完成：6 通过，0 失败，6 总计
⏱️  总耗时：1.09s
================================
```

---

## ✅ 通过标准

| 指标 | 要求 | 当前 |
|------|------|------|
| **必须测试通过率** | 100% | 6/6 |
| **总耗时** | <10s | ~1s |
| **崩溃次数** | 0 | 0 |

---

## 🔧 测试配置

**修改测试配置**：
```bash
# 编辑测试套件
nano test_suite.go

# 修改测试参数（如 TTL 时间、并发数等）
```

**添加新测试**：
```go
// 添加测试函数
func TestYourFeature(t *testing.T) {
    // 测试逻辑
}

// 在 main() 中注册
suite.RunTest("你的测试", func() error {
    t := &testing.T{}
    TestYourFeature(t)
    if t.Failed() {
        return fmt.Errorf("测试失败")
    }
    return nil
})
```

---

## 📝 测试最佳实践

### 1. 每次修改后运行测试

```bash
# 修改代码后
go run test_suite.go

# 确保所有测试通过
```

### 2. 添加新功能时加测试

```go
// 新功能
func NewFeature() { ... }

// 新测试
func TestNewFeature(t *testing.T) { ... }
```

### 3. 测试失败时

```bash
# 查看详细错误
go run test_suite.go -v

# 调试代码
# 修复后重新运行
```

---

## 🎯 测试覆盖率

**当前覆盖**：
- ✅ 缓存逻辑
- ✅ 配置加载
- ✅ 限流保护
- ✅ 错误处理

**待覆盖**：
- ⏳ API 调用（需要真实 API）
- ⏳ 语义匹配（未来功能）
- ⏳ 监控日志（需要完善）

---

**最后更新**: 2026-04-22  
**状态**: ✅ 测试套件完成
