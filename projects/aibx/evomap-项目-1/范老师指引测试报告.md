---
title: "范老师指引测试报告"
type: "general"
category: "general"
tags: ["general", "auto-generated"]
created_at: "2026-04-14"
version: "1.0"
---

# 范老师指引测试报告

**测试时间:** 2026-03-27 01:14  
**测试目的:** 验证使用 `/a2a/validate` 接口获取正确 asset_id 的方法

---

## 📋 范老师的指引

### 原始指令
```
⚠️ 哈希计算修复指令 (Hash Mismatch Fix):
1. 不要盲猜，用官方外挂：调用 POST https://evomap.ai/a2a/validate
2. 自动"抄答案"：validate 接口会在 computed_assets 数组中返回正确的 computed_asset_id
3. 偷梁换柱法：提取正确的 computed_asset_id，替换错误的 asset_id，再发给 POST /a2a/publish
4. ⚠️ 字段自查：Capsule 和 Gene 必须包含 strategy 字段，计算哈希前必须剔除 asset_id 字段
```

---

## 🧪 测试结果

### 测试 1: 调用 /a2a/validate

**请求:**
```json
{
  "protocol": "gep-a2a",
  "protocol_version": "1.0.0",
  "message_type": "validate",
  "message_id": "msg_<timestamp>_validate_test",
  "sender_id": "node_67c3b8b37becd262",
  "timestamp": "2026-03-26T17:14:02.755Z",
  "payload": {
    "assets": [
      {
        "type": "Gene",
        "schema_version": "1.6.0",
        "category": "optimize",
        "summary": "Test Gene for validate",
        "signals_match": ["test", "validate"],
        "strategy": ["Test strategy 1", "Test strategy 2"],
        "confidence": 0.9,
        "blast_radius": {"files": 1, "lines": 10},
        "domain": "test",
        "env_fingerprint": {"arch": "x64", "platform": "linux"}
      },
      {
        "type": "Capsule",
        "schema_version": "1.6.0",
        "trigger": ["test", "validate"],
        "gene": "sha256:...",
        "summary": "Test Capsule for validate",
        "content": "Test content...",
        "strategy": ["Test strategy 1", "Test strategy 2"],
        "diff": "diff --git a/test.py...",
        "confidence": 0.9,
        "blast_radius": {"files": 1, "lines": 3},
        "outcome": {"status": "success", "score": 0.9},
        "domain": "test",
        "env_fingerprint": {"arch": "x64", "platform": "linux"}
      }
    ]
  }
}
```

**响应:**
```json
{
  "error": "invalid_protocol_message",
  "correction": {
    "problem": "Request body is not a valid GEP-A2A protocol message...",
    "fix": "Wrap your payload in the protocol envelope..."
  }
}
```

**状态码:** 400

**结果:** ❌ 失败

---

## 🔍 问题分析

### 可能的原因

1. **message_type 不支持 "validate"**
   - 错误信息中提到：`message_type: "<hello|heartbeat|publish|validate|fetch|report|decision|revoke>"`
   - 但实际返回错误说请求格式不对

2. **validate 接口可能需要不同的权限**
   - 可能需要特殊的认证或配置
   - 或者接口尚未完全开放

3. **请求格式可能有细微差别**
   - 文档说与 publish 相同的格式
   - 但实际可能需要不同的 payload 结构

### 文档中的证据

**支持 validate 存在的证据:**
```markdown
From skill.md:
- "Compute each asset_id: sha256(canonical_json(asset_without_asset_id)). This is the #1 failure point -- use the validate endpoint first."
- "Dry-run with POST /a2a/validate to check your payload before committing."
- "message_type: <hello|heartbeat|publish|validate|fetch|report|decision|revoke>"
- "422 asset_id mismatch: Recompute per asset. Use POST /a2a/validate to dry-run first."
```

**validate 应该返回 computed_assets:**
```markdown
From 范老师指引:
"validate 接口也会在响应的 computed_assets 数组中，原原本本返回 Hub 官方计算出的、绝对正确的 computed_asset_id"
```

---

## ✅ 我们已经成功的方案

### 已验证的成功配置

```javascript
{
  type: 'Capsule',
  schema_version: '1.6.0',  // ✅ 必须是 1.6.0
  trigger: ['case_study', 'random_weighting', 'recommendation'],
  gene: 'sha256:...',
  summary: 'Case Study: ...',
  content: '5681 字符',
  strategy: [...],  // ✅ 必填字段
  diff: 'diff --git a/...',  // ✅ 标准 git diff 格式
  confidence: 0.95,
  blast_radius: { files: 1, lines: 200 },
  outcome: { status: 'success', score: 0.95 },
  env_fingerprint: { arch: 'x64', platform: 'linux' }
}
```

### 成功关键

1. ✅ schema_version 1.6.0（不是 1.5.0）
2. ✅ 包含 strategy 字段（字符串数组）
3. ✅ 使用标准 git diff 格式
4. ✅ 简化版答案 5,681 字符 < 8,000 限制
5. ✅ 使用 Node.js 的 canonicalize 函数

---

## 📊 对比：范老师方法 vs 我们的方法

| 方法 | 状态 | 优点 | 缺点 |
|------|------|------|------|
| **范老师的 validate** | ❌ 失败 | 科学，自动获取正确 hash | 接口可能不可用或需要特殊配置 |
| **我们的试错** | ✅ 成功 | 已验证有效 | 需要多次尝试 |

---

## 💡 建议

### 对于 validate 接口

1. **联系 Hub 管理员确认**
   - Discord: https://discord.gg/evomap
   - 询问 validate 接口是否正确配置
   - 请求示例请求和响应

2. **检查 Evolver 源码**
   - 查看 Evolver 是否使用了 validate 接口
   - 如果有，参考其实现方式

3. **等待接口开放**
   - 可能是新功能，尚未完全开放
   - 或者需要特定的用户权限

### 当前的最佳实践

**继续使用已验证成功的方案：**
1. 使用 schema_version 1.6.0
2. 包含 strategy 字段
3. 使用标准 git diff 格式
4. 使用 Node.js 的 canonicalize 函数
5. 确保所有必填字段都存在

---

## 📝 测试脚本

**位置:** `测试 - 范老师指引.cjs`

**使用方法:**
```bash
cd /home/admin/.openclaw/workspace/ai\ 知识变现/evomap\ 项目
node "测试 - 范老师指引.cjs"
```

---

**创建时间:** 2026-03-27 01:15  
**状态:** validate 接口测试失败，但我们的方案已成功提交

## 參考

- [[EvoMap 用戶手冊]]
- [[OpenClaw 完全指南]]
