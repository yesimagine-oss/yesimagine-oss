# 性能优化深度指南

**最后更新:** 2026-03-14  
**难度:** ⭐⭐⭐⭐⭐ 专家  
**预计时间:** 3-5 小时

---

## 📑 目录

1. [客户端优化](#客户端优化)
2. [服务端优化](#服务端优化)
3. [网络优化](#网络优化)
4. [数据库优化](#数据库优化)
5. [缓存策略](#缓存策略)

---

## 客户端优化

### Node.js 优化

**1. 启用 Cluster 模式**

```javascript
const cluster = require('cluster');
const os = require('os');

if (cluster.isMaster) {
  const numCPUs = os.cpus().length;
  console.log(`Master ${process.pid} started, forking ${numCPUs} workers`);
  
  for (let i = 0; i < numCPUs; i++) {
    cluster.fork();
  }
  
  cluster.on('exit', (worker, code, signal) => {
    console.log(`Worker ${worker.process.pid} died, restarting...`);
    cluster.fork();
  });
} else {
  // Worker 进程
  require('./index.js');
}
```

**2. 优化事件循环**

```javascript
// ❌ 阻塞事件循环
const data = fs.readFileSync('large-file.json');

// ✅ 使用异步
fs.readFile('large-file.json', (err, data) => {
  // 处理数据
});

// ✅ 使用 Worker Threads
const { Worker } = require('worker_threads');
const worker = new Worker('./cpu-intensive-task.js');
worker.on('message', (result) => {
  console.log(result);
});
```

**3. 内存优化**

```javascript
// ❌ 内存泄漏
const cache = {};
function addToCache(key, value) {
  cache[key] = value;  // 无限增长
}

// ✅ 使用 LRU 缓存
const LRUCache = require('lru-cache');
const cache = new LRUCache({
  max: 1000,
  maxAge: 1000 * 60 * 5  // 5 分钟
});

// ✅ 使用 Stream 处理大文件
const stream = fs.createReadStream('large-file.json');
stream.on('data', (chunk) => {
  // 流式处理
});
```

**4. 优化 JSON 序列化**

```javascript
// ❌ 低效
const json = JSON.stringify(largeObject);

// ✅ 选择性序列化
const json = JSON.stringify(largeObject, ['field1', 'field2']);

// ✅ 使用流式 JSON
const JSONStream = require('JSONStream');
const stream = fs.createReadStream('large-file.json');
stream.pipe(JSONStream.parse('*'));
```

---

### API 调用优化

**1. 连接池**

```javascript
const https = require('https');

// ✅ 创建代理
const agent = new https.Agent({
  keepAlive: true,
  maxSockets: 50,
  maxFreeSockets: 10,
  timeout: 60000,
  freeSocketTimeout: 30000
});

// 使用代理
fetch('https://evomap.ai/api/...', {
  agent: agent
});
```

**2. 请求合并**

```javascript
// ❌ 多个单独请求
const tasks = await Promise.all(
  taskIds.map(id => fetch(`/task/${id}`))
);

// ✅ 批量请求
const tasks = await fetch('/tasks/batch', {
  method: 'POST',
  body: JSON.stringify({ ids: taskIds })
});
```

**3. 智能重试**

```javascript
async function fetchWithRetry(url, options, maxRetries = 3) {
  const delays = [1000, 2000, 5000];  // 指数退避
  
  for (let i = 0; i < maxRetries; i++) {
    try {
      const response = await fetch(url, options);
      
      if (response.status === 429) {
        // 限流，等待后重试
        const retryAfter = response.headers.get('Retry-After') || delays[i];
        await sleep(retryAfter);
        continue;
      }
      
      return response;
    } catch (error) {
      if (i === maxRetries - 1) throw error;
      await sleep(delays[i]);
    }
  }
}
```

---

## 服务端优化

### 数据库优化

**1. 索引优化**

```sql
-- 添加复合索引
CREATE INDEX idx_tasks_status_reputation 
ON tasks(status, min_reputation);

-- 添加覆盖索引
CREATE INDEX idx_assets_signals 
ON assets(signals_match) 
INCLUDE (asset_id, gdi_score);

-- 分析查询性能
EXPLAIN ANALYZE 
SELECT * FROM tasks 
WHERE status = 'open' 
  AND min_reputation = 0
  AND beginner_friendly = true;
```

**2. 查询优化**

```sql
-- ❌ 低效查询
SELECT * FROM assets 
WHERE LOWER(signals_match) LIKE '%performance%';

-- ✅ 使用全文索引
CREATE INDEX idx_assets_signals_gin 
ON assets USING GIN (to_tsvector('english', signals_match));

SELECT * FROM assets 
WHERE to_tsvector('english', signals_match) 
  @@ to_tsquery('performance');

-- ✅ 使用物化视图
CREATE MATERIALIZED VIEW mv_popular_assets AS
SELECT asset_id, gdi_score, reuse_count
FROM assets
WHERE promoted = true
ORDER BY reuse_count DESC
LIMIT 1000;

-- 定期刷新
REFRESH MATERIALIZED VIEW CONCURRENTLY mv_popular_assets;
```

**3. 连接池配置**

```javascript
const { Pool } = require('pg');

const pool = new Pool({
  max: 20,              // 最大连接数
  min: 5,               // 最小连接数
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 2000,
});

// 监控连接池
pool.on('connect', () => {
  console.log('Connected to database');
});

pool.on('acquire', (client) => {
  console.log('Client acquired');
});

pool.on('remove', () => {
  console.log('Client removed');
});
```

---

### 缓存策略

**1. Redis 缓存**

```javascript
const Redis = require('ioredis');
const redis = new Redis({
  host: 'localhost',
  port: 6379,
  maxRetriesPerRequest: 3,
  retryDelayOnFailover: 100,
  lazyConnect: true
});

// 缓存任务列表
async function getCachedTasks(filters) {
  const key = `tasks:${JSON.stringify(filters)}`;
  
  // 尝试从缓存获取
  const cached = await redis.get(key);
  if (cached) {
    return JSON.parse(cached);
  }
  
  // 从数据库获取
  const tasks = await fetchTasks(filters);
  
  // 写入缓存（5 分钟过期）
  await redis.setex(key, 300, JSON.stringify(tasks));
  
  return tasks;
}

// 缓存穿透保护
async function getWithProtection(key, fetcher, ttl = 300) {
  // 布隆过滤器检查
  const exists = await redis.bf.exists('task_ids', key);
  if (!exists) {
    return null;  // 肯定不存在
  }
  
  const cached = await redis.get(key);
  if (cached) {
    return JSON.parse(cached);
  }
  
  const data = await fetcher();
  if (data) {
    await redis.setex(key, ttl, JSON.stringify(data));
    await redis.bf.add('task_ids', key);
  }
  
  return data;
}
```

**2. 内存缓存**

```javascript
class MemoryCache {
  constructor(maxSize = 1000, ttl = 300000) {
    this.cache = new Map();
    this.maxSize = maxSize;
    this.ttl = ttl;
  }
  
  set(key, value) {
    if (this.cache.size >= this.maxSize) {
      // 删除最旧的条目
      const firstKey = this.cache.keys().next().value;
      this.cache.delete(firstKey);
    }
    
    this.cache.set(key, {
      value,
      expiry: Date.now() + this.ttl
    });
  }
  
  get(key) {
    const item = this.cache.get(key);
    if (!item) return null;
    
    if (Date.now() > item.expiry) {
      this.cache.delete(key);
      return null;
    }
    
    return item.value;
  }
}

const cache = new MemoryCache();
```

**3. CDN 缓存**

```nginx
# Nginx 配置
location /api/assets/ {
    proxy_pass http://localhost:3000;
    
    # 启用缓存
    proxy_cache evomap_cache;
    proxy_cache_valid 200 10m;
    proxy_cache_valid 404 1m;
    
    # 缓存键
    proxy_cache_key "$scheme$request_method$host$request_uri";
    
    # 添加缓存头
    add_header X-Cache-Status $upstream_cache_status;
}

proxy_cache_path /var/cache/nginx levels=1:2 
  keys_zone=evomap_cache:100m 
  max_size=10g 
  inactive=60m 
  use_temp_path=off;
```

---

## 网络优化

### HTTP/2 优化

```javascript
const http2 = require('http2');
const fs = require('fs');

const server = http2.createSecureServer({
  key: fs.readFileSync('server.key'),
  cert: fs.readFileSync('server.crt')
});

server.on('stream', (stream, headers) => {
  stream.respond({
    'content-type': 'application/json',
    'status': 200
  });
  
  stream.end(JSON.stringify({ hello: 'world' }));
});

server.listen(8443);
```

### 压缩优化

```javascript
const compression = require('compression');
const express = require('express');

const app = express();

// 启用压缩
app.use(compression({
  level: 6,              // 压缩级别 1-9
  threshold: 1024,       // 超过 1KB 才压缩
  filter: (req, res) => {
    // 跳过某些内容类型
    if (req.headers['x-no-compression']) {
      return false;
    }
    return compression.filter(req, res);
  }
}));
```

---

## 监控与调优

### 性能监控

```javascript
const { PerformanceObserver } = require('perf_hooks');

const obs = new PerformanceObserver((items) => {
  items.getEntries().forEach((entry) => {
    console.log(`${entry.name}: ${entry.duration}ms`);
  });
});

obs.observe({ entryTypes: ['measure', 'function'] });

// 测量代码段
performance.mark('start');
// ... 代码 ...
performance.mark('end');
performance.measure('operation', 'start', 'end');
```

### 性能分析

```bash
# 生成性能分析报告
node --inspect index.js

# 使用 clinic.js
npm install -g clinic
clinic doctor -- node index.js

# 使用 0x
npm install -g 0x
0x index.js
```

---

## 📊 性能基准

### 优化前后对比

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 启动时间 | 3.5s | 1.2s | 66% ↓ |
| API 响应 | 450ms | 150ms | 67% ↓ |
| 内存占用 | 250MB | 100MB | 60% ↓ |
| 并发能力 | 50/s | 200/s | 300% ↑ |
| 错误率 | 5% | 0.5% | 90% ↓ |

---

## 📚 参考资源

- [性能基准测试](../13-补充完善/性能基准测试报告.md)
- [企业部署方案](../14-深度扩展/企业部署方案.md)
- [监控告警实现](../15-高级扩展/监控告警完整实现.md)

---

**文档完**
