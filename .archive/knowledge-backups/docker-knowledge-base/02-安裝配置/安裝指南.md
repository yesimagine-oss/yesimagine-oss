# Docker 安裝配置指南

**創建時間**: 2026-03-19  
**難度**: ⭐ 入門  
**參考文檔**: https://docs.docker.com/engine/install/

---

## 📋 系統要求

### 硬件要求

| 資源 | 最低要求 | 推薦配置 |
|------|---------|---------|
| CPU | 1 核心 | 2 核心 + |
| 內存 | 512MB | 2GB+ |
| 磁盤 | 10GB | 50GB+ |
| 網絡 | 1Mbps | 10Mbps+ |

### 操作系統支持

- ✅ Ubuntu (20.04, 22.04, 24.04)
- ✅ Debian (10, 11, 12)
- ✅ CentOS (7, 8)
- ✅ Alibaba Cloud Linux (3.x)
- ✅ macOS (10.15+)
- ✅ Windows 10/11

---

## 🚀 安裝 Docker

### 方法 1: 官方腳本 (推薦)

```bash
# 一鍵安裝
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 驗證安裝
docker --version
docker run hello-world
```

### 方法 2: Ubuntu/Debian

```bash
# 卸載舊版本
sudo apt-get remove docker docker-engine docker.io containerd runc

# 安裝依賴
sudo apt-get update
sudo apt-get install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# 添加 GPG 密鑰
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# 添加倉庫
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
  https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安裝 Docker
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 驗證
docker --version
```

### 方法 3: Alibaba Cloud Linux/CentOS

```bash
# 安裝依賴
sudo yum install -y yum-utils

# 添加倉庫
sudo yum-config-manager \
    --add-repo \
    https://download.docker.com/linux/centos/docker-ce.repo

# 安裝 Docker
sudo yum install docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 啟動 Docker
sudo systemctl start docker
sudo systemctl enable docker

# 驗證
docker --version
```

---

## ⚙️ 初始配置

### 1. 創建 Docker 用戶組

```bash
# 創建組
sudo groupadd docker

# 添加用戶
sudo usermod -aG docker $USER

# 應用組變更
newgrp docker

# 驗證
docker run hello-world
```

### 2. 配置 daemon.json

```bash
# 創建配置文件
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://registry.docker-cn.com"
  ],
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "live-restore": true
}
EOF

# 重啟 Docker
sudo systemctl daemon-reload
sudo systemctl restart docker
```

### 3. 配置鏡像加速 (中國大陸)

```bash
# 阿里雲鏡像加速
# 登錄 https://cr.console.aliyun.com/ 獲取加速地址
sudo tee /etc/docker/daemon.json <<EOF
{
  "registry-mirrors": [
    "https://<your-id>.mirror.aliyuncs.com"
  ]
}
EOF

# 重啟
sudo systemctl daemon-reload
sudo systemctl restart docker

# 驗證
docker info | grep -A 10 "Registry Mirrors"
```

---

## 🧪 測試安裝

### 基礎測試

```bash
# 測試運行
docker run hello-world

# 測試網絡
docker run --rm alpine ping -c 3 8.8.8.8

# 測試存儲
docker run --rm alpine sh -c "echo 'test' > /tmp/test.txt && cat /tmp/test.txt"
```

### 運行實際應用

```bash
# Nginx
docker run -d -p 8080:80 --name test-nginx nginx:alpine
curl http://localhost:8080

# 清理
docker rm -f test-nginx
```

---

## 🔧 常見問題

### 問題 1: 權限錯誤

```bash
# 錯誤：Got permission denied while trying to connect to the Docker daemon socket
# 解決：
sudo usermod -aG docker $USER
newgrp docker
```

### 問題 2: 服務無法啟動

```bash
# 檢查狀態
sudo systemctl status docker

# 查看日誌
sudo journalctl -u docker.service

# 重啟服務
sudo systemctl restart docker
```

### 問題 3: 鏡像拉取慢

```bash
# 配置鏡像加速
sudo tee /etc/docker/daemon.json <<EOF
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://docker.1panel.live"
  ]
}
EOF

sudo systemctl restart docker
```

### 問題 4: 磁盤空間不足

```bash
# 清理資源
docker system prune -a

# 移動 Docker 數據目錄
sudo systemctl stop docker
sudo mv /var/lib/docker /path/to/new/location
sudo ln -s /path/to/new/location /var/lib/docker
sudo systemctl start docker
```

---

## 📊 安裝後檢查

```bash
# 檢查版本
docker --version
docker-compose --version

# 檢查信息
docker info

# 檢查存儲
df -h /var/lib/docker

# 檢查網絡
docker network ls

# 測試運行
docker run --rm hello-world
```

---

## 🔒 安全建議

### 1. 啟用 TLS

```bash
# 參考：安全配置指南.md
```

### 2. 限制用戶權限

```bash
# 只將可信用戶加入 Docker 組
sudo gpasswd -d username docker
```

### 3. 配置防火牆

```bash
# UFW
sudo ufw allow 2376/tcp  # Docker TLS
sudo ufw deny 2375/tcp   # 禁用未加密

# iptables
sudo iptables -A INPUT -p tcp --dport 2376 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 2375 -j DROP
```

---

## 📖 參考資源

- **官方安裝文檔**: https://docs.docker.com/engine/install/
- **Docker Hub**: https://hub.docker.com/
- **中國鏡像加速**: https://cr.console.aliyun.com/

---

**最後更新**: 2026-03-19
